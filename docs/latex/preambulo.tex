% preambulo.tex — WBA Windows Toolkit
% Carregado via --include-in-header pelo pipeline Pandoc + LuaLaTeX.
%
% fontspec e babel[brazilian] são injetados automaticamente pelo template
% Pandoc quando pdf-engine=lualatex e lang=pt-BR estão definidos.
% Estão declarados abaixo apenas para documentação; descomente se este
% arquivo for usado fora do pipeline Pandoc:
%   \usepackage{fontspec}
%   \usepackage[brazilian]{babel}

% Geometria A4 com margens confortáveis
\usepackage[a4paper, top=2.5cm, bottom=2.5cm, left=2.5cm, right=2.5cm]{geometry}

% Quebra automática de linhas em blocos de código
% O ambiente "Highlighting" é gerado pelo Pandoc para syntax highlighting
\usepackage{fvextra}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{%
  breaklines=true,%
  breakanywhere=true,%
  breaksymbol={},%
  fontsize=\small,%
  commandchars=\\\{\}%
}

% Cores
\usepackage{xcolor}

% Links clicáveis
\usepackage[colorlinks=true, linkcolor=black, urlcolor=blue!60!black, citecolor=black]{hyperref}

% Tabelas com wrap automático de texto nas células
\usepackage{longtable}
\usepackage{etoolbox}
\usepackage{booktabs}
\usepackage{array}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

% Evita iniciar tabelas pequenas no espaço restante da página. Tabelas longas
% continuam podendo quebrar quando ultrapassarem uma página inteira.
\usepackage{needspace}
\BeforeBeginEnvironment{longtable}{\Needspace{18\baselineskip}}

% Cabeçalho e rodapé personalizados
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\textit{WBA Windows Toolkit}}
\fancyhead[R]{\small\textit{Manual do Operador}}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}
\raggedbottom

% Fonte monoespaçada para blocos de código e fonte principal sem serifa
% fontspec já foi carregado pelo template Pandoc neste ponto
\AtBeginDocument{%
  \setmainfont{Latin Modern Roman}%
  \setsansfont{Latin Modern Sans}%
  \setmonofont{Latin Modern Mono}[Scale=0.9,FakeBold=1.2]%
  \renewcommand{\familydefault}{\rmdefault}%
  \normalfont%
}
